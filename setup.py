from setuptools import setup

setup(name='secondary_locations_without_messages',
      version='0.1',
      description='secondary_locations_without_messages',
      url='http://example.com',
      author='proj',
      author_email='proj@example.com',
      license='MIT',
      packages=['sec_loc'],
      install_requires=['requests'],
      zip_safe=False)
